import TabPlaceholder from "./TabPlaceholder";
export default function StoreLocatorTab({ category }) {
  return <TabPlaceholder title="Store Locator" description="Search a location to find nearby craft supply stores." />;
}