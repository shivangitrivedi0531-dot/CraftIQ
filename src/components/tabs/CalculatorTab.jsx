import TabPlaceholder from "./TabPlaceholder";
export default function CalculatorTab({ category }) {
  return <TabPlaceholder title="Material Cost Calculator" description="Enter quantities to get total material cost and a suggested sell price." />;
}